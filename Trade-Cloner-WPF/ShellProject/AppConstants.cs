namespace ShellProject
{
    // This static class contains constants used throughout the application
    public static class AppConstants
    {
        // This constant defines the path to the JSON file storing trade data
        public const string jsonDataFilePath = @"trade_data.json";

        // This constant defines the date and time format for file naming
        public const string DateTimeFormatForFile = @"yyyyMMdd_HHmmss";

        // This constant defines the date and time format for display purposes
        public const string DateTimeFormatForDisplay = "dd-MM-yyyy hh:mm:ss tt";
    }
}
